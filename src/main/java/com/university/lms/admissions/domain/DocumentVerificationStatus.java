package com.university.lms.admissions.domain;

/** G5: whether admissions staff have checked an attached application document. */
public enum DocumentVerificationStatus {
    PENDING,
    VERIFIED,
    REJECTED
}
