package com.university.lms.document.domain;

/** Classification of a stored file, used for retention and access rules. */
public enum DocumentType {
    COURSE_MATERIAL,
    ASSESSMENT_SUBMISSION,
    TRANSCRIPT,
    IDENTIFICATION,
    CERTIFICATE,
    OTHER
}
