$kind: http-request
description: Full representation, including the description.
url: "{{baseUrl}}/api/v1/courses/{{courseId}}"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('200 OK', () => pm.response.to.have.status(200));
    language: text/javascript
order: 5000
